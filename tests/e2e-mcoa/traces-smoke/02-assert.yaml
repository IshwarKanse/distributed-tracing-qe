# Assert the ManagedClusterAddOn rolled out by verifying the resulting
# ManifestWork exists and was applied. Asserting on MCA.status.conditions
# directly is fragile (variable number of conditions over time).
---
apiVersion: work.open-cluster-management.io/v1
kind: ManifestWork
metadata:
  name: addon-multicluster-observability-addon-deploy-0
  namespace: local-cluster
